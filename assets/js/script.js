let currentCountWrapper = document.getElementById('currentCount');
let currentCount = 0;

function increment() {
    currentCount = currentCount + 1;
    currentCountWrapper.innerHTML = currentCount;
}


function decrement() {
    currentCount = currentCount - 1;
    currentCountWrapper.innerHTML = currentCount;
}

if (currentCount = 0) {
    currentCount==0;
}